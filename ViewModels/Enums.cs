namespace virtualReality.ViewModels
{
    public static class Enums
    {
        public enum OrderStatus
        {
            Ordered = 0,
            Dispatched = 1,
            Delivered = 2,
            Cancelled = 3
        }

        public enum Role
        {
            User = 0,
            Admin = 1
        }
    }
}
